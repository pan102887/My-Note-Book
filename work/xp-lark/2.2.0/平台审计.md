# 平台审计  

## 代码模块  

1. 切面
2. UIR辅助类
3. DAO

```java
        // 需要添加审计的特殊接口
        ArrayList<String> needToAudit = Lists.newArrayList(
                "/v1/api/cancelOrder/export",
                "/v1/api/commonCouponLog/export",
                "v1/api/commonCoupon/export",
                "/v1/api/cancelOrder/export",
                "/v1/api/couponCard/export",
                "/v1/api/CouponPackage/export",
                "/v1/api/marketCoupon/export");
        UriPatternHelper.INSTANCE.putUrisIntoNeedAuditSet(needToAudit);
```