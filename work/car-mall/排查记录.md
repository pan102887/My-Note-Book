```sql
/logan/configCacheSnapshot/listByIdBatch | select regexp_extract(message, '(?<=client-name=)([a-zA-Z0-9-])*', 0) as client, count(*) as request_count group by regexp_extract(message, '(?<=client-name=)([a-zA-Z0-9-])*', 0)
```